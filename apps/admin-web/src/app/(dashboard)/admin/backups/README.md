# Backup & Recovery UI

Platform-owner route: `/admin/backups`.

The page intentionally delegates all destructive checks to the protected API. Client-side confirmation is only an additional safeguard; the API independently requires the exact restore phrase and Platform Owner permission.
