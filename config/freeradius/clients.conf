client localhost {
	ipaddr = 127.0.0.1
	secret = ${env:RADIUS_SHARED_SECRET}
	shortname = localhost
}

# Production MikroTik NAS clients are loaded from the SQL `nas` table.
# Keep only localhost here for health checks and container diagnostics.
