client localhost {
	ipaddr = 127.0.0.1
	secret = radius-secret
	shortname = localhost
}

client city_centre_gateway {
	ipaddr = 10.10.0.1
	secret = radius-secret
	shortname = city-centre-gateway
	nastype = mikrotik
}

client nakawa_edge {
	ipaddr = 10.10.0.2
	secret = radius-secret
	shortname = nakawa-edge
	nastype = mikrotik
}
